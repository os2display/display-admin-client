import { api as generatedApi } from './api.generated'
import { fetchBaseQuery } from "@reduxjs/toolkit/query";

/**
 * Entry point for modifying api endpoint.
 */
export const api = generatedApi.enhanceEndpoints({
})
